spring:
    main:
        allow-circular-references: true
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
    datasource:
        url: jdbc:postgresql://localhost:5432/cafeteria
        username: postgres
        password: root
    flyway:
        baseline-on-migrate: true
server:
    port: 8080